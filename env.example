# ── Supabase ──────────────────────────────────────────────────────
# Find these in: Supabase dashboard → Project Settings → API

# Used by: backend, parser (server-side only — never expose to browser)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Used by: backend (JWT verification)
SUPABASE_ANON_KEY=eyJ...

# ── Frontend (Vite) ───────────────────────────────────────────────
# These are injected at BUILD time — safe to expose (anon key only)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# URL of the backend API — seen by the browser
# Dev:  http://localhost:2027
# Prod: https://your-api-domain.com
VITE_API_URL=http://localhost:2027

# ── Service ports (optional — defaults shown) ─────────────────────
# BACKEND_PORT=2027
# PARSER_PORT=2028
